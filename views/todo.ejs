<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="font-sans">
  <div class="mt-8 mb-8 w-full">
    <h1 class="text-2xl font-bold text-gray-800 mb-6">My Tasks</h1>
    <div class="bg-white shadow-xl rounded-xl overflow-hidden">
      <table class="w-full">
        <% for (var i = 0; i < getTodos.length; i++) { %>
        <tr class="hover:bg-gray-50 transition-colors duration-200 border-b border-gray-100 last:border-none">
          <td class="px-6 py-4 w-12">
            <input type="checkbox" class="w-4 h-4 rounded border-gray-300 text-blue-600 focus:ring-blue-500">
          </td>
          <th class="px-6 py-4 bg-gradient-to-r from-blue-500 to-blue-600 text-white font-medium text-left w-1/3">
            <%= `Task ${i + 1}` %>
          </th>
          <td class="px-6 py-4 text-gray-700 font-medium <%= getTodos[i].completed ? 'line-through text-gray-400' : '' %>">
            <%= getTodos[i].title %>
          </td>
        </tr>
        <% } %>
      </table>
    </div>
  </div>
</body>

</html>